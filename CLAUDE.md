# 작업 규칙

친구비 측정기 저장소에서 일할 때 지킬 것들.

## 문서가 코드에 앞선다

구조·필드·제한값을 바꿀 때는 **`mdfiles/` 의 문서를 먼저 고친다.** 특히 데이터 구조는 `데이터-계약-명세.md` 가 단일 정의처다. 같은 정의를 두 문서에 두지 않는다.

`backend/tests/test_docs_consistency.py` 가 문서·백엔드·프론트의 값을 비교한다. 어느 한쪽만 바꾸면 테스트가 깨진다. 깨지면 **어느 쪽이 틀렸는지 판단해서** 고친다. 테스트를 느슨하게 만들지 않는다.

## 절대 깨면 안 되는 것

| 규칙 | 깨지면 |
| --- | --- |
| OCR 엔진은 좌표와 이미지 크기를 반환해야 한다 | 화자 판별 불가 → 지표 절반이 계산 불가능 |
| 점수·비율·평균은 코드가 계산한다 | 같은 대화에 다른 점수가 나온다 |
| 대화 원문을 로그·응답에 남기지 않는다 | 개인정보 원칙 붕괴 |
| 대화는 구분자로 감싸고 내부 지시를 따르지 않는다 | 프롬프트 인젝션 |
| 입력을 학습에 쓰는 Provider를 채택하지 않는다 | 남의 사적 대화가 학습 데이터가 된다 |
| 고지 문구는 서버가 주입한다 | 모델이 문구를 바꾸거나 빠뜨린다 |

## 테스트

핵심 로직은 **테스트를 먼저 쓴다.** 실패를 확인하고 구현한다.

```bash
cd backend  && .venv/Scripts/python.exe -m pytest
cd frontend && npm test
```

Python은 **3.10 고정**이다. `StrictEnum`, `asyncio.timeout`, `datetime.UTC` 같은 3.11 문법을 쓰지 않는다.

## 코드 주석

**무엇을 하는지가 아니라 왜 그런지를 쓴다.** 코드를 읽으면 아는 것은 적지 않는다.

좋은 예:

```python
# 블록의 중심점이 아니라 여백을 비교하는 이유는, 긴 메시지가 화면 폭의 70%
# 이상을 차지해서 중심점이 화면 중앙 근처로 몰리기 때문이다.
```

## 실제 모델 붙이기

교체 지점은 설정뿐이다. 코드를 고치지 않는다.

```bash
FC_LLM_PROVIDER / FC_LLM_MODEL / FC_LLM_API_KEY
FC_OCR_ENGINE / FC_OCR_API_KEY
```

붙이기 전에 `backend/tools/evaluate_ocr.py` 와 `evaluate_llm.py` 로 실측한다. 절차는 `mdfiles/AI-모델-선정-보고서.md` 8장.

Provider를 바꾸면 **`mdfiles/운영-보안-법적고지-명세.md` 4.3의 제3자 제공 목록도 함께 고친다.** 코드만 바꾸고 고지를 방치하면 사실과 다른 안내가 된다.

## 배포 전제

- **단일 인스턴스, 단일 워커.** 작업 상태와 요청 제한이 프로세스 메모리에 있다.
- 늘려야 하면 `app/infrastructure/storage/base.py` 인터페이스를 외부 TTL 저장소 구현으로 갈아 끼운 뒤에 올린다.
- 공개 전 점검표는 `mdfiles/운영-보안-법적고지-명세.md` 8장.

## 커밋

- 무엇을 왜 바꿨는지 쓴다. 특히 **왜**.
- 결함을 고쳤으면 어떤 상황에서 어떻게 잘못됐는지 적는다.
- 문서와 코드를 함께 고쳤으면 한 커밋에 담는다.
